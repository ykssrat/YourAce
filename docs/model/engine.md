YourAce 历史评分器说明（待重写）

整理日期：2026-03-19
文档性质：历史说明

一、当前状态

这份文档原本描述的是 YourAce 早期“评分器主导”的实现思路。

但截至当前代码版本：

- 核心接口已移除 `score`
- 主判断输出已经切换为：
  - `label`
  - `horizon_signals`
  - `matrix`
- 当前系统更接近“子策略调度 + 三窗口看法矩阵”架构

因此，旧版评分器公式、打分区间、强烈看多/强烈看空阈值等内容，不再代表当前代码行为。

二、为什么保留本文件

保留这份文件的原因只有一个：

- 它仍然记录了项目早期从“连续分数”理解模型输出的历史设计过程

但请注意：

- 这不是当前接口说明
- 这不是当前移动端展示逻辑
- 这不是当前筛股脚本的筛选依据

三、当前应参考哪些文档

如果你想看当前有效口径，请优先查看：

- `docs/model/blackbox_interface.md`
- `docs/test/backend_api.md`
- `docs/ui/app_feature_design.md`
- `docs/version/release_0.1.5.md`
- `docs/version/release_0.1.6.md`

四、后续重写方向

这份文档后续建议重写为：

- 子策略调度说明
- 三窗口看法生成逻辑
- `label` 与 `matrix` 的关系
- `selected_features` 的解释层作用
- Lean 与轻量策略之间的验证关系

五、当前结论

一句话理解：

- 旧版是“评分器解释模型”
- 当前是“矩阵解释模型”

在正式重写前，请不要再把本文件当作当前代码行为说明。
